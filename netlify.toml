# NUTZ · Forms — Netlify deploy config
# הטפסים הם קבצי HTML עצמאיים (פונטים ו-CSS מוטמעים), אין שלב build.
#
# הנתיבים הציבוריים:
#   https://nutz-forms.netlify.app/intake/form   שאלון קבלת מתאמן
#   https://nutz-forms.netlify.app/health/form   הצהרת בריאות
#
# כללי הפניה (אותיות גדולות, נתיבים מקוצרים) יושבים ב-dist/_redirects,
# כדי שיעבדו גם בהעלאה ידנית של התיקייה וגם בפריסה מ-Git.

[build]
  publish = "dist"
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
